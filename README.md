# Py-Viz
A visualization tool for visualizing the hyperspectral images in both 2D & 3D.

## Getting Started
Install the packages & source it.
```bash
python -m venv .venv

source .venv/bin/activate
```

Run the program
```bash
python -m main
```

